apiVersion: v1
kind: Service
metadata:
  name: {{.ServiceName}}
  namespace: production
  labels:
    app: {{.ServiceName}}
spec:
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
  selector:
    app: {{.ServiceName}}
  type: ClusterIP
